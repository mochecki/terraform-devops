name: Setup TF state store INT

on:
  workflow_dispatch:

env:
  RESOURCE_LOCATION: 'West Europe'

jobs:
  deploy:
    uses: ./.github/workflows/workflow_setup_statestore.yml
    with: 
      environment: 'test'
    secrets: inherit
    

